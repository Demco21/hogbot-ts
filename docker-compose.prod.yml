services:
  hogbot:
    image: hogbot-ts # prebuilt image, needs to be built prior
    container_name: hogbot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
